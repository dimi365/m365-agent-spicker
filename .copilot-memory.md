# m365-agent-spicker – Projekt-Wissen (Copilot Memory)

## Projektpfad
`C:\Users\TAAMODM1\OneDrive - Swisscom\Desktop\Notes\VS Code Projects\m365-agent-spicker\`

## Struktur
```
m365-agent-spicker/
├── README.md
├── .gitignore
├── onepager-standalone/index.html   ← 1-Datei-Version (HTML+CSS inline)
└── webapp/
    ├── index.html                   ← Markup (getrennt)
    └── css/styles.css               ← Fluent-2-Styles
```

## Herkunft / Inhalt
- Basiert auf `Downloads\Agent Spicker M365.docx` (Word-Datei, verifiziert + korrigiert 13.07.2026).
- Backup: `Downloads\Agent Spicker M365 (Backup).docx`
- Gerenderte Standalone-Version: `Downloads\Agent Spicker M365 - One Pager.html`

## Korrekturen gegenüber Original (verifiziert gegen MS Docs, Stand 13.07.2026)
1. **SharePoint Agent + Listen**: SP Agents nutzen laut offizieller FAQ **keine** Listendaten.
   Werkzeug = Copilot Studio (nicht SharePoint Agent custom).
2. **SharePoint-Listen in Copilot Studio**: max. **15 Listen** (empfohlen 10),
   max. **35'000 Rows/Liste**, insgesamt max. **120'000 Rows**.
3. **Analytische Fragen**: List-Abfragen nur erste **2'048 Zeilen**; kein Code-Ausführen möglich.
4. Alle anderen Werte bestätigt: 500 KS/Agent, 20 Source Items (SP Agent custom).

## Design-System
- Fluent 2: Segoe UI, Brand `#0F6CBD`, Gradient-Header, Zebra-Tabelle,
  Pills (`#EFF6FC` / `#0F548C`), Warn-Tag (`#FFF4CE`), Print: A4 Querformat.

## GitHub / Deployment (geplant, noch nicht ausgeführt)
- Ziel: GitHub Pages, Custom Root-Domain (keine github.io-URL sichtbar).
- Strategie: User-Site-Repo `<user>.github.io` = Monorepo → `deinedomain.ch/`
- DNS: A-Records 185.199.108-111.153, CNAME www → github.io.
- GitHub CLI (`gh`) installiert, aber noch **nicht authentifiziert**.
- Noch offen: GitHub-Username, echte Domain, `git init` + Actions-Workflow anlegen.

## Nächste Schritte
1. GitHub-Username + Domain nennen.
2. `gh auth login` (User selbst, im Browser).
3. Copilot: `git init`, `CNAME`, `deploy.yml`, `gh repo create`, push.
4. User: DNS A-Records beim Domain-Provider setzen.
5. GitHub Pages Custom Domain aktivieren + SSL abwarten.
